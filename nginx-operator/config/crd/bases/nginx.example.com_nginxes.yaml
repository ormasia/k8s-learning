---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: nginxes.nginx.example.com
spec:
  group: nginx.example.com
  names:
    kind: Nginx
    listKind: NginxList
    plural: nginxes
    singular: nginx
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Nginx is the Schema for the nginxes API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: NginxSpec 定义 Nginx 实例的期望配置
            properties:
              image:
                description: Nginx 镜像版本（默认 nginx:1.23）
                type: string
              replicas:
                description: 副本数（默认 1）
                format: int32
                type: integer
              servicePort:
                description: 服务暴露端口（默认 80）
                format: int32
                type: integer
            type: object
          status:
            description: NginxStatus 定义 Nginx 实例的实际状态
            properties:
              readyReplicas:
                description: 当前运行的副本数
                format: int32
                type: integer
              serviceAddress:
                description: 服务访问地址（格式：<service-name>.<namespace>:<port>）
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
